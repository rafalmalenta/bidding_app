<div class="side_nav">
<ul>
{% if parent %}
    <li>Go back to <a href="{{ path('auctions_category', {'category': parent.name }) }}" > {{ parent.name }} </a></li>
{% endif %}
{% for category in categories %}
    <li><a href="{{ path('auctions_category', {'category': category.name }) }}" > {{ category.name }} </a></li>
{% endfor %}
</ul>
</div>