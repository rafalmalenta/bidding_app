<nav >

    <ul class="menu">
        <li class="brand nav-item">
            <a class="nav-link" href="{{ path('main')}}">Bidding app</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('auctions_category', {'category': 'all' }) }}">View auctions </a>
        </li>
        {% if not app.user %}
        <li class="nav-item">
            <a class="nav-link" href="{{ path('app_login') }}">Log in</a>
        </li>
        <li class="nav-item">
            <a class="nav-link" href="{{ path('app_register') }}">Register account</a>
        </li>
        {% endif %}
        {% if app.user %}
        <li class="dropdown nav-item">
            <a class="nav-link dropdown-toggle" href="#"  role="button" aria-haspopup="true" aria-expanded="false">
                Welcome {{ app.user.email }}
            </a>
            <div class="dropdown-menu" >
                <a class="dropdown-item" href="{{path('app_logout')}}">Logout</a>
                <a class="dropdown-item" href="{{path('account')}}">My account</a>
            </div>
        {% endif %}
        </li>
    </ul>

</nav>